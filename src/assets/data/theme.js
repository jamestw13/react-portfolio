import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#020c1b',
    },
    secondary: {
      main: '#00e676',
    },
    background: {
      default: '#020c1b',
      paper: '#eeffff',
    },
    text: {
      primary: '#FFF',
    },
  },
});

export default theme;
